"""Utility package marker.

Keep this initializer side-effect free so lightweight helpers such as
``utils.path_config`` can be imported without loading optional plotting,
training, or checkpoint dependencies.
"""

# histogram_preprocessor moved to archive_histogram_matching/
